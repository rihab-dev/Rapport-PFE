\rowcolors{0}{}{}
\arrayrulecolor{black}
\renewcommand{\arraystretch}{1.3}
\small
\begin{longtable}{|C{1cm}|L{6.5cm}|L{7cm}|C{1cm}|}
\caption{Backlog du sprint 2}\label{tab:backlog_sprint2}\\
\hline
\textbf{ID US} & \textbf{User Story} & \textbf{Description technique} & \textbf{Effort} \\
\hline
\endfirsthead

    \hline

\endhead

\hline
\endfoot

\hline
\endlastfoot
\multicolumn{4}{|l|}{\cellcolor{blue!20}\textbf{Epic 5 - Gestion des Clients}} \\
\hline
 US17 & En tant qu'\textbf{administrateur}, je souhaite créer un profil client afin de centraliser ses informations. &
\vspace{0.5mm}
\begin{itemize}[nosep, left=0pt, label={$\bullet$}]
  \item Développer l'endpoint {\footnotesize "POST /api/clients"}
    pour créer simultanément le compte utilisateur
     et le profil entreprise
  \item Implémenter le service de récupération automatique
    de logo à partir du nom de la
    société, avec upload manuel via
    {\footnotesize "POST /api/clients/:id/logo"}
    (traitement Sharp, formats JPG/PNG/GIF/WEBP, 2 Mo max)
  \item Intégrer le service de géocodage via
    {\footnotesize "GET /api/clients/geocode?q="} pour
    l'autocomplétion d'adresse (ArcGIS, rate limit
    20 req/min via \textbf{@nestjs/throttler})
  \item Réaliser le formulaire de création côté Next.js
\end{itemize} & 1 \\
\hline

US18 & En tant qu'administrateur, je souhaite consulter la liste des clients afin de visualiser les clients existants. &

\begin{itemize}[nosep, left=0pt, label={$\bullet$}]
  \item Développer l'endpoint{\footnotesize "GET /api/clients"} avec filtrage RBAC selon le rôle de l'utilisateur connecté
  \item Développer les endpoints de données enrichies par client : {\footnotesize"GET /api/clients/:id/projets","GET /api/clients/:id/contrats","GET /api/clients/:id/demandes","GET /api/clients/:id/tickets"}
  \item Réaliser l'interface de visualisation avec vue
    grille et vue liste côté Next.js
\end{itemize} & 2 \\
\hline

US19 & En tant qu'\textbf{administrateur}, je souhaite consulter le profil détaillé d'un client afin de visualiser toutes ses informations. &
\begin{itemize}[nosep, left=0pt, label={$\bullet$}]
  \item Développer l'endpoint {\footnotesize "GET /api/clients/:id"}
     pour récupérer
    les informations complètes du client
  \item Développer l'endpoint {\footnotesize "GET /api/clients/:id/business-overview"}
    pour la vue métier consolidée (projets contextuels,
    contrats, demandes)
  \item Développer l'endpoint
    {\footnotesize "GET /api/clients/:id/verifier-desactivation"}
    pour vérifier les conditions avant désactivation
  \item Réaliser l'interface de profil structurée en onglets
    côté Next.js
\end{itemize} & 1 \\
\hline

US20 & En tant qu'\textbf{administrateur}, je souhaite modifier les informations d'un client afin de maintenir des données à jour. &
\begin{itemize}[nosep, left=0pt, label={$\bullet$}]
  \item Développer l'endpoint {\footnotesize "PUT /api/clients/:id"}
     pour la mise à
    jour des informations du client et de son profil
    entreprise
  \item Valider les formats de données côté serveur via
    \textbf{class-validator}
  \item Gérer la mise à jour du logo via
    {\footnotesize "POST /api/clients/:id/logo"} et la suppression
    via {\footnotesize "DELETE /api/clients/:id/logo"}
  \item Réaliser l'interface d'édition côté Next.js
\end{itemize} & 2 \\
\hline

US21 & En tant qu'\textbf{administrateur}, je souhaite activer et désactiver un compte client afin de contrôler l'accès. &
\begin{itemize}[nosep, left=0pt, label={$\bullet$}]
  \item Développer l'endpoint
    {\footnotesize "PATCH /api/clients/:id/desactiver"}
     et
    {\footnotesize "PATCH /api/clients/:id/reactiver"}
  \item Implémenter la vérification préalable via
    {\footnotesize "GET /api/clients/:id/verifier-desactivation"}
    pour bloquer l'opération si le client possède des
    contrats actifs, des demandes en attente ou des
    tickets non résolus
  \item Intégrer \textbf{EmailService} pour l'envoi
    automatique de notifications de changement de statut
  \item Réaliser les boutons d'action avec modale de
    confirmation côté Next.js
\end{itemize} & 3 \\
\hline

US22 & En tant qu'\textbf{administrateur}, je souhaite imprimer l'annuaire d'un client afin de disposer d'une version papier. &
\begin{itemize}[nosep, left=0pt, label={$\bullet$}]
  \item Implémenter l'export CSV des données client
    (informations société, contrats, projets) via le
    composant \textbf{ExportCSV} côté frontend
  \item Utiliser la bibliothèque \textbf{xlsx} pour la
    génération des fichiers tabulaires
  \item Réaliser le bouton d'export dans l'interface
    de liste des clients
\end{itemize} & 1 \\
\hline

US23 & En tant qu'\textbf{administrateur}, je souhaite créer un projet rattaché à un client afin d'organiser les interventions. &

\begin{itemize}[nosep, left=0pt, label={$\bullet$}]
  \item Développer l'endpoint {\footnotesize "POST /api/projets"}
     avec validation
    des données via \textbf{class-validator}
  \item Gérer la relation projet-client
    et l'attribution optionnelle
    d'un chef de projet
  \item Envoyer des notifications aux membres de l'équipe
    lors de la création
  \item Réaliser le formulaire de création côté Next.js
\end{itemize} & 3 \\
\hline

US24 & En tant qu'\textbf{administrateur}, je souhaite consulter la liste des projets afin de piloter l'activité. &

\begin{itemize}[nosep, left=0pt, label={$\bullet$}]
  \item Développer l'endpoint {\footnotesize "GET /api/projets"}
      avec filtres par
    \textbf{search}, \textbf{tags}, \textbf{statutContrat}
    et \textbf{statutP}
  \item Appliquer le contrôle d'accès RBAC selon le rôle :
    ADMIN voit tous les projets, CHEF\_PROJET voit ses
    projets, DEVELOPPEUR voit ses projets affectés,
    CLIENT voit ses propres projets
  \item Réaliser l'interface avec tableau et filtres
    avancés côté Next.js
\end{itemize} & 2 \\
\hline

US25 & En tant qu'\textbf{administrateur}, je souhaite affecter des développeurs à un projet afin de constituer l'équipe. &

\begin{itemize}[nosep, left=0pt, label={$\bullet$}]
  \item Développer les endpoints de gestion d'équipe :
   {\footnotesize "POST /api/projets/:id/equipe"} (ajouter),
    {\footnotesize "PUT /api/projets/:id/equipe/:employeId"}
    (modifier le rôle),
    {\footnotesize "DELETE /api/projets/:id/equipe/:employeId"}
  \item Implémenter le workflow de proposition/validation :
    {\footnotesize "POST /api/projets/:id/assigner"},
    {\footnotesize "POST /api/projets/affectation/:id/valider"},
    {\footnotesize "POST /api/projets/affectation/:id/refuser"}
  \item Développer les endpoints de suivi :
    {\footnotesize "GET /api/projets/mes-affectations-envoyees"}
    et {\footnotesize "GET /api/projets/statistiques-affectations"}
  \item Envoyer des notifications en temps réel à chaque
    étape du workflow
  \item Réaliser les interfaces de gestion d'équipe
    côté Next.js
\end{itemize} & 3 \\
\hline

US26 & En tant que \textbf{membre d'équipe}, je souhaite consulter les informations du projet afin de comprendre le contexte. &

\begin{itemize}[nosep, left=0pt, label={$\bullet$}]
  \item Développer l'endpoint {\footnotesize "GET /api/projets/:id"}
    avec contrôle d'accès
    selon le rôle de l'utilisateur
  \item Développer {\footnotesize "GET /api/projets/:id/equipe"}
     pour consulter
    les membres et leur charge
  \item Développer {\footnotesize "GET /api/projets/:id/charge"}
    pour visualiser la charge de l'équipe
  \item Réaliser la page de détail du projet organisée
    en sections côté Next.js
\end{itemize} & 1 \\
\hline

% Epic 2: Gestion des utilisateurs
\multicolumn{4}{|l|}{\cellcolor{blue!20}\textbf{Epic 5 - Gestion des Contrats}} \\
\hline

US28 & En tant qu'administrateur, je souhaite créer un contrat de maintenance afin de formaliser les engagements de service avec le client. &

\begin{itemize}[nosep, left=0pt, label={$\bullet$}]
  \item Développer l'endpoint {\footnotesize "POST /api/contrats"}
     avec upload
    de fichier via \textbf{FileInterceptor} (PDF, Word,
    Excel, 10 Mo max) et hashage anti-doublon
  \item Développer l'endpoint d'extraction PDF
    {\footnotesize "POST /api/contrats/extract-pdf"}
     via
    \textbf{pdf-parse} + regex pour récupérer
    automatiquement référence, dates, montants, SLA,
    client et projet
  \item Développer l'endpoint d'upload de document scanné
    {\footnotesize "POST /api/contrats/upload-scanne"},
   (formats PDF/JPG/PNG)
  \item Réaliser l'interface avec choix du mode
    (formulaire manuel ou extraction PDF) et
    pré-remplissage automatique côté Next.js
\end{itemize} & 2 \\
\hline

US29 & En tant qu'administrateur, je souhaite consulter la liste des contrats afin de suivre les engagements contractuels. &

\begin{itemize}[nosep, left=0pt, label={$\bullet$}]
  \item Développer l'endpoint {\footnotesize "GET /api/contrats"}
  \item Développer les endpoints auxiliaires :
    {\footnotesize "GET /api/contrats/clients"},
    {\footnotesize "GET /api/contrats/projets?clientId="},
    {\footnotesize "GET /api/contrats/demandes?clientId=\&projetId="},
    {\footnotesize "GET /api/contrats/archived"},
    {\footnotesize "GET /api/contrats/client/:clientId"}
  \item Développer {\footnotesize "GET /api/contrats/:id/fichier"}
    pour le téléchargement du fichier contrat avec
    détection automatique du type MIME et streaming
  \item Réaliser l'interface avec tableau, filtres, badges
    de statut et actions rapides côté Next.js
\end{itemize} & 3 \\
\hline

US30 & En tant qu'administrateur, je souhaite recevoir des alertes avant l'expiration des contrats afin d'anticiper les renouvellements &

\begin{itemize}[nosep, left=0pt, label={$\bullet$}]
  \item Développer le CRON job d'alertes automatiques
    déclenchant des notifications à \textbf{J-30, J-15
    et J-7} avant expiration (champs
    \textbf{alerte30JoursEnvoyee},
    \textbf{alerte15JoursEnvoyee},
    \textbf{alerte7JoursEnvoyee} en base pour éviter
    les doublons)
  \item Envoyer les notifications par e-mail
    (\textbf{Nodemailer}) et in-app
    (\textbf{NotificationsService})
  \item Développer {\footnotesize "GET /api/contrats/expiration/status"}
    pour récupérer le statut d'expiration des contrats
    du client connecté
  \item Réaliser l'interface d'alertes côté Next.js
\end{itemize} & 2 \\
\hline

US31 & En tant qu'administrateur, je souhaite archiver un contrat afin de conserver l'historique. &

\begin{itemize}[nosep, left=0pt, label={$\bullet$}]
  \item Développer l'endpoint
    {\footnotesize "PATCH /api/contrats/:id/archive"}
     avec motif
    obligatoire (\textbf{minimum 10 caractères})
  \item Développer l'endpoint de restauration
    {\footnotesize "PATCH /api/contrats/:id/restore"}
  \item Développer l'endpoint de suppression définitive
    {\footnotesize "DELETE /api/contrats/:id"}
  \item Conserver les données historiques avec
    \textbf{motifArchivage} et \textbf{dateArchivage}
  \item Réaliser le dialogue de confirmation côté Next.js
\end{itemize} & 3 \\
\hline

US32 & En tant que client, je souhaite demander la résiliation de mon contrat afin d'interrompre le service. &

\begin{itemize}[nosep, left=0pt, label={$\bullet$}]
  \item Développer l'endpoint
    {\footnotesize "POST /api/contrats/:id/demande-resiliation"}
    avec motif obligatoire (minimum 10 caractères) et
    e-mail du client requis
  \item Envoyer une notification par e-mail aux
    administrateurs et mettre à jour les champs
    \textbf{dateDemandeResiliation} et
    \textbf{motifResiliation} en base
  \item Développer {\footnotesize "GET /api/contrats/demandes-resiliation"}
     pour que
    l'admin consulte les demandes en attente
  \item Réaliser le formulaire de demande avec motif
    obligatoire côté Next.js
\end{itemize} & 1 \\
\hline

US33 & En tant qu'administrateur, je souhaite résilier un contrat afin de l'interrompre avant terme. &
\par

\begin{itemize}[nosep, left=0pt, label={$\bullet$}]
  \item Développer l'endpoint
    {\footnotesize "PATCH /api/contrats/:id/resilier"}
     avec motif
    obligatoire (minimum 10 caractères)
  \item Mettre à jour le statut du contrat et notifier
    le client par e-mail (\textbf{Nodemailer})
  \item Journaliser l'action via \textbf{ActivityLog}
  \item Réaliser le dialogue de résiliation avec
    confirmation côté Next.js
\end{itemize} & 2 \\
\hline

US34 & En tant que chef de projet, je souhaite consulter les contrats dans un calendrier afin de visualiser les dates d'expiration &

\begin{itemize}[nosep, left=0pt, label={$\bullet$}]
  \item Utiliser l'endpoint {\footnotesize "GET /api/contrats"}
    avec filtrage côté frontend
  \item Calculer les indicateurs côté frontend :
    contrats expirant bientôt (J-30), contrats actifs,
    code couleur selon le statut
  \item Réaliser la vue calendrier avec affichage des
    contrats par mois côté Next.js
\end{itemize} & 1 \\
\hline

US35 & En tant que \textbf{client}, je souhaite soumettre un retour d'expérience à l'expiration de mon contrat afin d'exprimer ma satisfaction. &
\par

\begin{itemize}[nosep, left=0pt, label={$\bullet$}]
  \item Développer l'endpoint
    {\footnotesize "POST /api/contrats/:id/feedback"}
     pour
    enregistrer la note, le commentaire et l'intention
    de renouvellement
  \item Vérifier que le profil client existe
     avant
    d'enregistrer le feedback
  \item Implémenter la notification automatique aux
    administrateurs si le client souhaite renouveler
  \item Journaliser l'action via \textbf{ActivityLog}
  \item Réaliser le formulaire de feedback côté client
    avec affichage conditionnel selon le statut du contrat
\end{itemize} & 1 \\
\hline

US36 & En tant qu'\textbf{administrateur}, je souhaite générer un PDF formaté du contrat afin de le partager avec le client. &

\begin{itemize}[nosep, left=0pt, label={$\bullet$}]
  \item Développer l'endpoint de génération PDF en utilisant la bibliothèque \textbf{PDFMake}
  \item Inclure les informations du contrat : référence, client, projet, dates, montants (HT, TVA, TTC), type SLA et conditions
  \item Utiliser le template HTML existant comme base de mise en page
  \item Réaliser le bouton de génération et de téléchargement côté Next.js
\end{itemize} & 2 \\
\hline

% Epic 3: Gestion des rôles et permissions
\multicolumn{4}{|l|}{\cellcolor{blue!20}\textbf{Epic 14 - Consulter journal d'activité}} \\
\hline

US101 & En tant qu'administrateur, je souhaite consulter l'historique complet de toutes les actions afin d'assurer la traçabilité. &

\begin{itemize}[nosep, left=0pt, label={$\bullet$}]
  \item Développer l'endpoint
   {\footnotesize "GET /api/activity-logs"}
     avec pagination
    (page, limit), filtres
    (userId, entityType,
    action, status,
    startDate, endDate) et recherche
    textuelle
  \item Développer {\footnotesize "GET /api/activity-logs/stats"}
    pour les statistiques globales (taux de succès,
    actions fréquentes, activité horaire)
  \item Développer {\footnotesize "GET /api/activity-logs/user/:userId"}
    et {\footnotesize "GET /api/activity-logs/entity/:type/:id"}
    pour les vues filtrées par utilisateur ou entité
  \item Implémenter la sanitisation automatique des
    métadonnées sensibles
  \item Réaliser l'interface avec tableau filtrable,
    timeline et vue détaillée côté Next.js
\end{itemize} & 3 \\
\hline

US102 & En tant qu'administrateur, je souhaite exporter les logs d'audit afin de répondre aux exigences de conformité. &

\begin{itemize}[nosep, left=0pt, label={$\bullet$}]
  \item Développer l'endpoint
    {\footnotesize "GET /api/activity-logs/me/export"}
    avec formats \textbf{CSV} et \textbf{JSON},
    filtres personnalisables et limite de
    \textbf{10 000 enregistrements}
  \item Implémenter le nettoyage automatique des logs
    anciens via CRON job quotidien à 2h du matin
  \item Développer l'endpoint de nettoyage manuel
    {\footnotesize "POST /api/activity-logs/cleanup"}
  \item Réaliser l'interface d'export avec sélection
    de format et téléchargement direct côté Next.js
\end{itemize} & 2 \\
\hline

\end{longtable}